# CLAUDE.md

This file gives Claude guidance when working in this repository (via the
GitHub Action `@claude` mentions and automatic PR reviews).

## Project overview

`claudenew` — describe what this project does here as it grows.

## How to interact with Claude

- Mention `@claude` in any issue or pull request comment to ask Claude to
  help: answer a question, write code, fix a bug, or open a PR.
- Every pull request is automatically reviewed by Claude when it is opened
  or updated.

## Conventions

Update these as the project takes shape so Claude follows your standards:

- **Style:** match the existing code in the file you are editing.
- **Commits:** use clear, descriptive commit messages.
- **Tests:** add or update tests for any behavior change when a test suite
  exists.
- **Scope:** keep changes focused on the request; avoid unrelated edits.

## What Claude should focus on in reviews

- Correctness and potential bugs
- Security concerns
- Readability and maintainability
- Test coverage for new behavior

## Build / test commands

Add the commands to build, run, and test this project here once they exist,
for example:

```bash
# install dependencies
# run tests
# start the app
```
